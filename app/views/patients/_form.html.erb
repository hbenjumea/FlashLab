<%= form_for(@patient) do |f| %>
  <% if @patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
      <% @patient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nip %><br>
    <%= f.number_field :nip %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :born %><br>
    <%= f.date_select :born, :start_year => 1900 %>
  </div>
  <div class="field">
    <%= f.label :genre %><br>
    <%= f.select :genre, options_for_select(['Masculino','Femenino']) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
